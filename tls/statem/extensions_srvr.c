

#include "tls_locl.h"
#include "statem_locl.h"
#include "packet_locl.h"

int
tls_parse_ctos_etm(TLS *s, PACKET *pkt, uint32_t context, X509 *x,
                    size_t chainidx)
{
    return 1;
}
